{
  "name": "cubing-competition",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbo --port 2500",
    "build": "next build",
    "start": "next start",
    "lint": "npx tsc && next lint --fix && npx stylelint \"**/*.css\" --fix && npx markdownlint **/*.md -f --ignore node_modules",
    "prettier": "prettier . --write",
    "prepare": "pnpm run prettier && pnpm run lint"
  },
  "dependencies": {
    "@types/dompurify": "^3.0.5",
    "dompurify": "^3.1.6",
    "next": "14.2.8",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.8",
    "typescript": "^5",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.1",
    "markdownlint-cli": "^0.41.0"
  }
}
